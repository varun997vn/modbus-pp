#include "modbus_pp/core/timestamp.hpp"

// Timestamp is fully implemented in the header (inline/constexpr).
// This translation unit exists to ensure the header compiles independently
// and to provide a link target if needed in the future.
